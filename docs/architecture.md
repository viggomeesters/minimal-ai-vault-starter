# Architecture

```text
human daily/inbox notes -> raw events -> typed JSONL records -> bundles/views/indexes
```

Markdown input is evidence. JSONL records are canonical. SQLite and Markdown views are generated projections.
